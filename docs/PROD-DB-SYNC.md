# PROD-DB-SYNC — що з міграцій живе в проді

Єдине місце, де будь-який агент (виконавець, аудиторка, Lovable) дивиться стан
бази **без питань до власниці**. Перевіряється скриптом:

```
node scripts/check-db-sync.mjs
```

## Як воно працює під капотом

1. Файл у `supabase/migrations/` — лише текст. У прод він потрапляє **тільки**
   коли власниця вставить його в чат Lovable («виконай у базі точно як є»).
2. Після застосування Lovable записує **свій хеш-файл** `YYYYMMDDHHMMSS_<uuid>.sql`
   (це водяний знак) і перегенеровує `src/integrations/supabase/types.ts`
   із **живої** бази.
3. Тому `types.ts` — єдине об'єктивне дзеркало проду. Міграція з таймстемпом
   **нижче** останнього хеш-файлу буде **мовчки пропущена** (ordering trap).

## Другий доказ — прогін (22.09)

`types.ts` каже, ЩО є в проді; `npm run db:replay` каже, ЯК воно поводиться: уся історія
`supabase/migrations/` збирається на чистому Postgres (`scripts/db-replay/`), схема звіряється
з `types.ts` (0 розбіжностей), кожен `onConflict` у коді — з унікальними ключами, а сценарії
`scenarios/*.sql` ганяють тригери й RPC (самолікування виплат, видалення акаунта, лог
нагадувань). Файли, які в прод не потрапляли ніколи або падають на чистому Postgres з
відомої причини, перелічені з доказом у `scripts/db-replay/history.json` — жоден із них не
може стояти ВИЩЕ водяного знаку. Нова міграція без сценарію = тригер, який живе на слово.

## Правила для агентів

- Новий таймстемп завжди **вище** останнього хеш-файлу Lovable (`check-db-sync`
  показує його першим рядком). Інакше — перейменувати (`git mv`), не чекати.
- Кожна міграція, що додає колонку/функцію, несе рядок
  `-- LIVE-MARKER: <фрагмент, який має з'явитись у types.ts>`. Скрипт звіряє
  його з `types.ts` і каже `✅ live` / `⛔ NOT live` — це і є відповідь «чи вже».
- **Межа методу (аудит 03.09).** `types.ts` описує ФОРМУ схеми, не її тіло.
  Міграція, що перевипускає наявне в'ю чи функцію з тією самою сигнатурою,
  збігається з маркером ЩЕ ДО застосування — скрипт скаже «✅ live» про те,
  чого в проді немає. Такі міграції несуть `-- LIVE-MARKER-NONE: <як перевірити
  вручну>`, скрипт друкує «❓ не доводиться», а стан береться з цього журналу.
- Ідемпотентність обов'язкова (`IF EXISTS` / `OR REPLACE` / `ON CONFLICT`):
  власниця не може перевірити, що вже запускала, — SQL сам мусить бути байдужим.
- SQL для власниці — **у чат, цілком**, не «скопіюй з репо».
- Після Run: коли Lovable оновить `types.ts`, агент запускає `check-db-sync`,
  бачить `✅ live` і переводить рядок нижче у ✅. Лише тоді — «done».

## Журнал

| Міграція | Що | Як перевірити (types.ts) | Стан |
|---|---|---|---|
| `< 20260903081642` (107 файлів) | історія до 03.09 — застосована через пайплайн Lovable у свій час | схема в `types.ts` | ✅ історія |
| `20260903081642_a4976b0d…` | M1: `source` immutable + гейт цін групових (аудит 02.09) | хеш-файл Lovable | ✅ |
| `20260903100000_lessons_visible_currency` | M4: `currency` у `lessons_visible` (валюта пари) | `lessons_visible` Row має `currency` | ✅ 03.09 (Lovable `150928`) |
| `20260903110000_money_by_currency` | M3: `manager_debts_by_currency()`, `get_people_aggregates` + `unpaid_by_currency` | `manager_debts_by_currency`, `unpaid_by_currency` у types | ✅ 03.09 (Lovable `151318`) |
| `20260903150000_finances_period_totals` | M2 (справжній): підсумки /finances рахує база через `lessons_visible` (INVOKER) | `finances_period_totals` у types | ✅ 03.09 (Lovable `151432`) |
| `20260903210000_fix_visible_dup_and_totals_grant` | ⛔ Аудит 03.09: `lessons_visible` дублювала уроки пари з кількома предметами (дохід і борг подвоєні); `finances_period_totals` падала з 42501 у кожного (читала базову `lesson_participants`); лог нагадувань не приймав жодного рядка (CHECK + UNIQUE) | **не доводиться з types.ts** (перевипуск в'ю і функції). Вручну: `SELECT count(*) FROM lessons_visible WHERE id='<урок пари з 2 предметами>'` = 1; виклик `finances_period_totals` від звичайного репетитора не 42501 | ✅ 03.09 (Lovable `210021`) |
| `20260903170000` / `20260903180000` (драфти «хаб = акаунт менеджера») | ЗНЯТО 07.09: суперечили рішенню власниці 31.08 «школа = окрема сутність»; замінено трьома міграціями нижче | — | ✂️ видалено з репо |
| `20260904100000_debt_conducted_only_and_topup_date` | Борг = проведений і не оплачений (обидві хабові функції) · `wallet_topup` приймає дату внесення | `wallet_topup` Args мають `_paid_at` | ✅ 05.09 (Lovable `044807`) |
| `20260905120000_chat_thread_manager_scope` | П1.3: менеджер поза тредом не відкриває пари САМОСТІЙНОГО репетитора (support-треди з менеджером-стороною — як раніше) | **не доводиться з types.ts** (перевипуск функції). Вручну: менеджер → `get_or_create_chat_thread(<незалежний>, <його учень>)` = помилка 'Not allowed'; хабова пара = id треду | ✅ live (Lovable-хеш `20260905095032` містить обидві функції слово в слово) |
| `20260905130000_people_aggregates_debt_model` | Аудит 05.09: «Люди» рахували борг за СКАСОВАНОЮ моделлю (будь-який неоплачений, майбутні включно; штраф губився). Предикат = проведене + штраф — як financials.ts / manager_debts_* | **не доводиться з types.ts** (перевипуск функції). Вручну: учениця з (700 проведений, 350 штраф, 1050 майбутній) у «Людях» = Борг 1 050 (2), не 2 100 (3) | ✅ live (Lovable-хеш `20260905095032` містить обидві функції слово в слово) |
| `20260910110000_db_backup_bucket_and_cron` | Бекапи (премортем п.8): приватний бакет db-backups (без жодної політики — лише service role) + pg_cron 23:45 UTC → edge db-backup (gzip-знімок 24 бізнес-таблиць, ретенція 30 днів) | **не доводиться з types.ts** (бакет і cron.job поза types). Вручну: зранку в Storage → db-backups має бути backup-YYYY-MM-DD.json.gz | ✅ live 11.09 З ОГОВОРКОЮ — Lovable-хеш `20260911112028` містить ЛИШЕ cron (`db-backup-nightly`, 23:45 UTC). Рядок `INSERT INTO storage.buckets ('db-backups')` з нашого файла Lovable **не переніс** — імовірно його рушій не пише у `storage.*`. Отже: розклад є, а чи є БАКЕТ — не доведено нічим. Щоб питання зникло назавжди, edge `db-backup` тепер створює бакет сам, якщо його немає (правка 11.09, потребує редеплою `db-backup`). Файл міграції лишається нижче водяного знаку — повторно НЕ вставляти, він уже не спрацює. Доказ роботи: наступного ранку у Storage → `db-backups` має лежати `backup-YYYY-MM-DD.json.gz`. ✅ стоїть тому, що ВЕСЬ зміст файла тепер покритий: cron — хешем Lovable, бакет — самою edge-функцією; самому файлу більше нічого робити, і повторно вставляти його НЕ треба |
| `20260907100000_hub_entity_model` | ХАБ етап A: `hubs`, `hub_managers`, `hub_members`, `settings.hub_id` (привілейована) + предикати `is_hub_scoped`/`is_hub_member`/`is_manager_of_*` + бекфіл єдиної школи + рядки settings для хабових репетиторів без них + `merge_pending_profile` переносить школу + роль manager лише від суперадміна + `create_hub`/`rename_hub`/`move_tutor_to_hub` + `start_manager_chat`/`notify_managers` через школу + засів `platform_admins` по обох поштах | `hubs`, `hub_managers`, `hub_members` Row у types; `tutor_workspace_settings` Row має `hub_id`; `create_hub` у types | ✅ 09.09 (Lovable `20260909083313`; копія = наш файл + guard `EXISTS profiles` у двох бекфілах — один осиротілий рядок `user_roles` (tutor без профілю) пропущено, інакше FK падав — + явні GRANT на три таблиці) |
| `20260907110000_hub_scope_policies` | ХАБ етап B: 78 manager-політик + 3 DEFINER-в'ю (`lessons_visible`, `lesson_participants_visible`, `group_enrollments_visible`) скоуплено на школу; платформенне → суперадмін | **не доводиться з types.ts**. Вручну: `SELECT count(*) FROM pg_policies WHERE qual ILIKE '%is_hub_%' OR with_check ILIKE '%is_hub_%'` ≥ 70 | ✅ live 09.09 — Lovable-хеш `20260909083633` (80 політик + 3 вʼю) |
| `20260907120000_hub_scope_rpcs` | ХАБ етап C: 21 SECURITY DEFINER-функція перевіряє школу (`is_hub_manager_of`); мертві `get_lesson_financials`/`list_lesson_financials` видалено | `get_lesson_financials` ЗНИКАЄ з types; `is_hub_manager_of` зʼявляється | ✅ live 09.09 — Lovable-хеш `20260909084000` (22 функції, доведено тілом) |
| `20260907125000_hub_scope_addendum` | ХАБ доповнення (07.09, знайдено етапом D на репліці): `feedback_submissions` — 2 політики без лапок (звернення = платформенне → суперадмін); `set_group_enrollment_price` / `set_group_participant_payment` — школа репетитора групи; `is_group_tutor` / `is_group_active_student` — менеджер питає лише про свою школу; `create_notification` — менеджер сповіщає лише свою школу, менеджера — лише його школа (суперадміна — будь-хто) | **не доводиться з types.ts** (нових об'єктів немає) — доводиться етапом D: `✅ Хаб-скоуп: чисто` | ✅ live 09.09 — Lovable-хеш `20260909084143` (6 армів, що знайшов етап D) |
| `20260907130000_hub_scope_assert` | ХАБ етап D (аудит 07.09): ЧИСТА перевірка живої бази — сканує `pg_policies`, `pg_proc` і `pg_views` на manager-арми без хабового предиката і падає списком. Свіп A–C доводиться по ФАЙЛУ, тож політика, якої у файлі немає, лишилась би без скоупу мовчки (так ux-step49 проминув три назви). Уточнено 07.09: арм = справжня `has_role(…, 'manager')`, не будь-яка згадка слова; тригери не перевіряються; `is_hub_manager_of` / `hub_of_user` / `is_manager_of_*` — прийнятий скоуп; + DEFINER-в'ю | **не доводиться з types.ts** (нічого не створює). Успіх = `NOTICE ✅ Хаб-скоуп: чисто`; провал = перелік політик/функцій/в'ю | ✅ live 09.09 — виконано; знайшов 6 армів → доповнення застосовано |
| `20260907150000_carried_over_import` | Імпорт «усе, що є» (07.09): `lessons.carried_over` (перенесений борг = cancelled + штраф + carried_over — у грошах є, у «проведено»/серіях/бейджах нема), `lessons_visible` + `lesson_details_student` віддають колонку, RPC `import_student_bundle` (учень + борг + передоплата + розклад на 4 тижні, серверний замок `is_tutor_pro`), `award_my_badges` рахує штрафи як борг, `add_or_link_independent_student` приймає учня без прізвища (NOT NULL падав) | `lessons` Row має `carried_over`; `import_student_bundle` у types | ✅ live 09.09 — Lovable-хеш `20260909084633` (`import_student_bundle` у types.ts) |
| `20260910100000_landing_funnel_anon` | Воронка лендінгу для НЕзареєстрованих (10.09): таблиця-лічильник `landing_funnel_daily` (день × крок → hits + суми; жодного user_id, IP чи пристрою) + `log_landing_event` (SECURITY DEFINER, білий список із 5 імен, числа обрізані по стелі) для `anon`. Прямі INSERT/UPDATE відкликані в усіх — писати можна лише через функцію. Читає суперадмін (картка «Воронка лендінгу» в адмінці) | `landing_funnel_daily` Row у types; `log_landing_event` у types | ✅ live 10.09 — Lovable-хеш `20260910080449` (копія = наш файл слово в слово) |
| `20260911120000_landing_telegram_handoff` | Дайджест у Telegram ДО реєстрації (11.09): `landing_handoffs` (список + готовий текст дайджесту під токен `lh_…`, 24 год, без політик — лише service role і DEFINER-RPC; IP — лише md5 для ліміту 5/год), RPC `create_landing_handoff` / `read_landing_handoff` / `landing_bot_username` (anon), `telegram_bot_state.bot_username` (бот пише сам через getMe), тригер `on_auth_user_created_landing_handoff` — реєстрація з токеном прив'язує chat_id і вмикає ранковий дайджест; cron `landing-handoffs-cleanup` 03:15 UTC | `landing_handoffs` Row, `create_landing_handoff` у types; `telegram_bot_state` Row має `bot_username` | ✅ live 11.09 — Lovable-хеш `20260911111732` (`landing_handoffs` + 3 RPC у types.ts; тригер і cron у тілі хеш-файла). Едж `telegram-poll` передеплоєно тим же кроком |
| `20260913090000_service_role_rpc_grants` | Edge-функції під service role → явний `GRANT EXECUTE … TO service_role` на 16 RPC, які вони викликають (13.09). Симптом: `confirm-pending-signup` → `permission denied for function is_pending_email` → запрошений учень не прив'язується. Права anon/authenticated не чіпаються | **не доводиться з types.ts** (гранти поза types). Вручну: `SELECT has_function_privilege('service_role','public.is_pending_email(text)','EXECUTE')` → `true` | ✅ live 13.09 — Lovable-хеш `20260913133308` (копія = наш файл слово в слово, усі 16 сигнатур звірено з історією `CREATE FUNCTION`: збіг повний, тож `undefined_function` не мав спрацювати в жодному рядку). Наш файл лишається **нижче** водяного знаку — повторно НЕ вставляти, весь його зміст уже покритий хеш-файлом. Остаточний доказ — `has_function_privilege` по всіх 16 (запит у чаті 13.09); поки він не показаний, стан тримається на тому, що Lovable пише хеш-файл лише після успішного Run |
| `20260913200000_payout_autofill_selfheal` | Виплата репетитору на хабовому уроці не лишається порожньою (13.09, «пропала ставка у Петра Городного»): `pick_tutor_payout(tutor, subject)` — єдиний вибір ставки (по предмету без регістру/пробілів → профіль → єдина ставка); `autofill_lesson_details_prices` перевипущено як BEFORE INSERT **OR UPDATE OF tutor_payout** (NULL/0 на невиплаченому хабовому уроці → ставка); ланцюг `trg_lessons_ensure_details` → `trg_lesson_details_autofill` перевипущено ідемпотентно; разовий бекфіл усіх хабових нескасованих невиплачених уроків із порожньою виплатою і наявною ставкою (усі школи, не лише 4 уроки Петра). Ціну учня разово не чіпає. `pick_tutor_payout` — EXECUTE лише service_role (маржа школи) | **не доводиться з types.ts** (тригери). Вручну: `SELECT count(*) FROM pg_trigger WHERE tgname IN ('trg_lesson_details_autofill','trg_lessons_ensure_details')` → 2; NOTICE `payout selfheal: заповнено виплат на N уроках` | ✅ live 13.09 — Lovable застосував її своїм хеш-файлом `20260913211716` (він тепер водяний знак), `pick_tutor_payout` є в `types.ts`. **Файл `20260913200000` приходить із пушем уже НИЖЧЕ знаку — повторно вставляти НЕ треба, і `lovable-paste/10-виплати-самолікування.sql` теж.** Побічний наслідок, знайдений аудиторкою прогоном 364 міграцій на локальному Postgres 16: `trg_lessons_ensure_details` створює рядок деталей одразу з `student_price = 0, tutor_payout = 0`, тож «не задано» більше НЕ представиме як NULL — через це в менеджера зникало попередження «ставку не задано». Клієнт вирівняно комітом `79686efd` (ознака `hasAmount`, пробник `demo/audit/zero-money.mjs`, два ратчети). Якщо колись знадобиться чесний NULL у даних — це окрема міграція, не терміново. Перевірено на Postgres 16 локально: бекфіл 3/3 (регістр, профільна ставка, скасований і виплачений не чіпаються), INSERT → 600/300, UPDATE 0/NULL → ставка, свідома сума лишається, незалежний і репетитор без ставки — без вигадок, повторний запуск 0 |
| `20260914090000_fix_purge_user_data` | Видалення акаунта було зламане для ВСІХ (14.09, скарга живого користувача: «зареєструвалась помилково як учень, видалити акаунт не можу — пише кудись писати особисто»). У `purge_user_data` один рядок із 53 звертався до `marketing_unsubscribe_tokens.user_id` — колонки, якої там немає (таблиця ключується `email`). PL/pgSQL падає на ньому, транзакція відкочується ЦІЛКОМ, edge `delete-account` віддає 500, клієнт показує «сервіс недоступний». Тобто акаунт не міг видалити НІХТО — ні учень, ні репетитор. Для сторів це пряма вимога (App Store 5.1.1(v), Google Play) | **не доводиться з types.ts** (тіло функції). Вручну: `SELECT public.purge_user_data('<uuid тестового акаунта>')` → без помилки | ✅ live 15.09 — власниця вставила SQL у чат Lovable, той застосував його своїм хеш-файлом `20260915080758_d8506129-e821-4c65-be2a-1f88cb917c5f` (він тепер водяний знак) і закомітив у `main`. **Наш файл `20260914090000` приходить із пушем уже НИЖЧЕ знаку — повторно вставляти НЕ треба.** Доведено двома способами: (1) читальна перевірка `5-що-справді-полагоджено.sql` на ЖИВІЙ базі — «✅ полагоджено, видалення проходить, пошта звільняється»; (2) наскрізний прогон на локальному Postgres 16 з повною історією (364 міграції): створено користувача з профілем, контактами й токеном розсилки, викликано `purge_user_data` так само, як це робить edge (службова роль, `auth.uid()` = NULL) → без помилки, всі три записи зникли. **Пастка при перевірці:** перша версія діагностики шукала `marketing_unsubscribe_tokens … user_id = _user_id` і давала ХИБНУ тривогу на щойно полагодженій базі — у новій версії всередині підзапиту стоїть `c.user_id = _user_id`. Ознака виправленої версії — `WHERE email IN`, і тільки вона |
| `20260915100000_payment_reminder_kinds` | Нагадування про оплату мовчки не працювали (15.09, знайдено на ЖИВИХ даних: 69 боргів, 68 старші за добу, НУЛЬ автоматичних нагадувань за 30 днів — при успішному погодинному кроні). Три правки в одному файлі: (1) CHECK приймає нові види `debt_1..debt_4` для нагадувань про борг (раз на 3 дні, максимум 4 — рішення власниці 15.09); (2) UNIQUE логу доповнено `student_id` — без нього з ГРУПОВОГО уроку в лог потрапляв лише перший учасник, а решта конфліктували й випадали, тож їхній дедуп не працював ніколи; (3) частковий індекс по парі для швидкого пошуку `debt%`. **Контекст, без якого рядок не читається:** крон писав у лог вид `before_1d`/`after_1d`, якого старий CHECK не приймав — вставка падала МОВЧКИ, а дедуп читає саме цей лог, тож поки урок був у своєму вікні, учень отримував нагадування ЩОГОДИНИ з 9 до 21. Ручна кнопка писала `manual` і тому працювала — саме тому в логу було рівно два рядки. Код тепер пише лише дозволені значення, а помилку вставки не ковтає. | **не доводиться з types.ts** (CHECK та індекси поза types). Вручну: `INSERT ... reminder_kind='debt_2'` проходить; `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='lesson_payment_reminders_reminder_kind_check'` містить `debt_4` | ✅ live 16.09 — Lovable застосував як хеш `20260916182739` (той самий зміст: CHECK із `debt_1..debt_4`, UNIQUE із `student_id`, частковий індекс `debt%`); файл `20260915100000` тепер НИЖЧЕ знаку — повторно не вставляти. Перевірено на локальному Postgres 16 з повною історією: застосовується начисто, повторний запуск ідемпотентний, вбудована перевірка в кінці файла падає, якщо CHECK не оновився. Частоту нагадувань перевірено вісьмома модульними тестами, кожен ратчет — зламуванням коду |
| `20260924120000_claim_tutor_role` | Реєстрація через Google робила з репетитора УЧНЯ (аудит шляхів 24.09): OAuth не несе нашої ролі (метадані пише провайдер), тож `handle_new_user` бачить порожнє `role` і ставить `student`. Людина з лендінгу ДЛЯ РЕПЕТИТОРІВ опинялась в учнівському застосунку без дороги назад (ролі міняє лише менеджер) і без вставленого списку учнів. Нова RPC `claim_tutor_role()` (SECURITY DEFINER, EXECUTE лише `authenticated`): лише про себе, лише 24 години після реєстрації, лише коли акаунт НЕ має учнівських даних (`student_rates`, `lessons`, `hub_members`) і ролі менеджера; ставить роль `tutor`, самостійний воркспейс і 30-денний тріал — як у реєстрації поштою. `guard_user_roles_writes` перевипущено з однією новою гілкою — транзакційним прапорцем `app.allow_initial_role`, який ставить лише ця функція (клієнт GUC не задає; гілка стоїть ПІСЛЯ заборони на роль менеджера, тож менеджера через неї не взяти) | **LIVE-MARKER:** `claim_tutor_role` у `types.ts` | ✅ live 24.09 — Lovable застосував як хеш `20260924151830` (зміст = наш файл без блоку коментаря; `claim_tutor_role` є в `types.ts`; файл `20260924120000` тепер НИЖЧЕ знаку — повторно не вставляти). Перевірено прогоном усієї історії (351 міграція) на локальному Postgres 16 + сценарій `50-claim-tutor-role.sql`: свіжий акаунт стає репетитором (воркспейс + тріал), акаунт із учнівськими даними — `has_data`, старший за добу — `too_late`, самопризначення менеджера — виняток. Мутація «прибрати перевірку чистого акаунта» → сценарій червоний |
| `20260924160000_payout_protect_before_autofill` | «Самолюк — ставку не задано» (24.09, повторювана скарга власниці; живі дані: ставка 350 з травня, уроки від менеджера = 350, уроки, які репетиторка поставила САМА 14/18/22.09 = `tutor_payout NULL`, `tutor_payout_status NULL`). Причина: на `lesson_details` два BEFORE INSERT тригери, Postgres виконує їх за абеткою імені — `trg_lesson_details_autofill` підставляє ставку, а `trg_protect_lesson_details_payout_insert` (20.06, «не-менеджер не вписує виплату сам») біжить ДРУГИМ і стирає її в NULL/NULL. Тобто кожен урок, створений репетитором, народжувався без виплати; повторне збереження ставки цього не лікувало. Файл: (1) захист перейменовано у `trg_00_protect_lesson_details_payout_insert` — тепер стоїть ПЕРЕД autofill; (2) захист лишає статус `'unpaid'`, не NULL (NULL ховав урок від суми «до виплати»); (3) разове лікування: статуси NULL→unpaid + виплата зі ставки для всіх хабових нескасованих невиплачених уроків із порожньою виплатою; (4) вбудована перевірка порядку. Захист маржі не слабшає: сума береться лише зі ставок менеджера | **не доводиться з types.ts** (тригер). Вручну: `SELECT tgname FROM pg_trigger WHERE tgrelid='public.lesson_details'::regclass AND tgname LIKE '%protect_lesson_details_payout_insert'` → `trg_00_protect_lesson_details_payout_insert`; NOTICE `payout protect-order: статусів NULL→unpaid N, виплат заповнено зі ставок M` | ⏳ чекає на вставку в Lovable (24.09). Доведено на локальному Postgres 16 з повною історією: сценарій `60-payout-when-tutor-creates-lesson.sql` (урок від репетитора під ролью authenticated → до міграції NULL/NULL, після — 350/unpaid; від менеджера 350/unpaid; довільну суму репетитор не впише; порядок тригерів); «прод сьогодні» → міграція → зіпсований рядок вилікувано (1 статус, 1 виплата); повторний запуск ідемпотентний (0/0). Сценарій 10 цього не бачив, бо створював уроки як postgres (auth.uid() = NULL → захист мовчить) |
