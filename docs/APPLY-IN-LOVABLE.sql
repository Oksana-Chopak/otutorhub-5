-- ⚠️ ЦЕЙ ФАЙЛ БІЛЬШЕ НЕ ВИКОРИСТОВУЙ ОДНИМ ШМАТКОМ.
-- Попередня спроба зупинялась на блоці pg_temp (предмети), через що таблиця
-- звернень не створювалась. Тепер скрипт розбито на 3 безпечні частини:
--   docs/APPLY-PART-1-tables.sql     ← виконай ПЕРШОЮ (таблиці/колонки)
--   docs/APPLY-PART-2-functions.sql  ← потім (функції + view)
--   docs/APPLY-PART-3-data.sql       ← останньою (відновлення цін + предмети)
-- Кожну частину окремим повідомленням у чат Lovable:
--   «Виконай у базі даних цей SQL точно як є, без змін: <вміст частини>»
