# ПРОГРАМА РЕДИЗАЙНУ ФОРМ — єдиний план (оновлено після повного перепису поколінь)

## Факт-карта (доказова, з коду)
НОВЕ покоління (затверджений 58px-стиль): QuickLessonDialog (усі 3 тьютор/менеджер
варіанти), LessonDetailsDialog, OnboardingFlowB. ВСЕ ІНШЕ — старе покоління.

## Хвилі (кожна = окремий коміт + маркер у робота «ДЕПЛОЙ СВІЖИЙ»)
✅ Х1. Урок-створення: manager/hub/independent → QuickLessonDialog (зроблено).
▶ Х2 (ПЕРШИЙ коміт наступної сесії). ОПЛАТА — RecordPaymentSheet → новий стиль,
   логіка 1:1: таби «Урок/Передоплата», режими «уроками/сумою», пара-пікер,
   маркування paid, rpc wallet_topup. 447 рядків — робиться на свіжому контексті.
Х3. ЛЮДИ-СТВОРЕННЯ: новий StudentUpsertDialog (58px) з режимами
   tutor-quick (замінює QuickAddStudentDialog: rpc add_or_link_independent_student
   + send-student-invite), independent-повний (замінює форму «Мої учні»
   create+edit), manager (вибір репетитора, замінює People-sheet учня).
   Зносить 3 старі форми, розтяжка на єдиність.
Х4. УЧЕНЬ+ГРУПИ: бронювання учня (інлайн pending-гілка) → новий стиль;
   діалог створення групи; запити переносу (обгортки SLA/TCRC).
Х5. Люди-запрошення (tutor/manager sheets), WalletDialog, AvailabilityManager.

## Правила виконання
- Логіка/данні при рестайлі не змінюються (payload-и і rpc — байт-у-байт).
- Кожна хвиля: parse-гейт → повна батарея → пуш → «підтягни main» → Publish →
  робот підтверджує маркером; власниця дивиться лише кінцевий вигляд.
